extends layout

block styling
  - css("../css/main.css");

block body
  #main-1-col
    h2 #{ctx.title}
    article
      - foreach (line; ctx.content)
        p #{line}
    p
      a(href="../index.html") Back home
